# Autocomplete example

## Getting Started

```
git clone git@github.com:hoitomt/firehose_autocomplete.git
cd firehose_autocomplete
bundle install

# Update the database.yml to match your credentials before running the db create and migrate

bundle exec rake db:create
bundle exec rake db:migrate
bundle exec rake db:seed

bundle exec rails server
```
